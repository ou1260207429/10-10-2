import { TimeNewStringPipe } from './time-new-string.pipe';

describe('TimeNewStringPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeNewStringPipe();
    expect(pipe).toBeTruthy();
  });
});
