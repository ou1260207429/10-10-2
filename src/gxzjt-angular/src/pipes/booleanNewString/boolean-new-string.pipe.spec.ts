import { BooleanNewStringPipe } from './boolean-new-string.pipe';

describe('BooleanNewStringPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanNewStringPipe();
    expect(pipe).toBeTruthy();
  });
});
